[build]
  functions = "netlify/functions"
  publish = "."
  command = "npm install"

[functions]
  node_bundler = "esbuild"

# entries.js가 Functions v2의 config.path로 /api/entries를 직접 처리하므로
# 별도의 리다이렉트 규칙은 필요하지 않습니다.
